# Database
DB_PASSWORD=yeschef_secret_2024

# JWT: reemplazar por una clave fuerte en produccion (>= 32 caracteres). No usar este valor.
JWT_KEY=yeschef-dev-key-not-for-production

# CORS: origenes permitidos separados por comas (por defecto se permite cualquier origen)
CORS_ORIGINS=http://localhost:4200

# Mercado Pago (opcional): sin token el pago online funciona en modo simulado (sin pasarela real).
MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=